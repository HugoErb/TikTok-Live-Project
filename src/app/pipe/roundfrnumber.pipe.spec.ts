import { RoundfrnumberPipe } from './roundfrnumber.pipe';

describe('RoundfrnumberPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundfrnumberPipe();
    expect(pipe).toBeTruthy();
  });
});
